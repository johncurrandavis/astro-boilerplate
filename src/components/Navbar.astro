---
import ThemeSelector from './ThemeSelector.jsx';
---

<nav class="flex items-center justify-between p-4 bg-[hsl(var(--card))] text-[hsl(var(--card-foreground))]">
  <div class="font-bold text-xl">My Astro Site</div>
  <ThemeSelector client:load /> <!-- client:load enables Preact -->
</nav>
